# Rust build output
target/

# Tauri generated
gen/

# Bun-compiled sidecar binaries (regenerated by `pnpm build:sidecar`).
# Each dev must build for their own architecture; committing a 62MB binary
# per triple would bloat the repo and would still need rebuilding on any
# engine source change.
binaries/

# Platform icons we don't ship (Mac-only for now)
icons/android/
icons/ios/

# macOS
.DS_Store
