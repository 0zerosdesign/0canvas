{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 1A-2 capabilities. Expanded per sub-phase as features land.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
