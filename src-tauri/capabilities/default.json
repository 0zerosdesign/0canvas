{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 1C capabilities. Expanded per sub-phase as features land.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "pty:default"
  ]
}
